{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "Begin basic CRUD app",
  "scripts": {
    "build": "deno bundle src/browser.tsx > dist/browser.js",
    "start": "sandbox"
  },
  "devDependencies": {
    "@architect/sandbox": "latest",
    "cross-env": "^7.0.2",
    "mkdirp": "^1.0.4"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}